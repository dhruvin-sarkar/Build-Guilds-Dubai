import Showcase from '../components/Showcase';

function BuildsPage() {
  return (
    <main id="top" className="appMain" aria-label="Build Guild Dubai project builds">
      <Showcase />
    </main>
  );
}

export default BuildsPage;
